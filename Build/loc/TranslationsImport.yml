# ==================================================================================
# Pipeline for VsCodeExtension-Localization build definition
# Gets translated files from VSCodeExt and creates a PR for vscode-cpptools
#
# Triggered only by daily schedule
# ==================================================================================

trigger: none

pool:
  vmImage: 'windows-latest'

steps:
- checkout: self
  path: s/vscode-cpptools

- task: CmdLine@2
  inputs:
    script: 'pushd && cd .. && git clone --progress --verbose https://$(OneLocBuildPat)@ceapex.visualstudio.com/CEINTL/_git/VSCodeExt && popd'

- task: CmdLine@2
  inputs:
    script: 'pushd && cd Extension && node ./translations_auto_pr.js microsoft vscode-cpptools cpptools csigs $(csigsPat) $(Build.SourcesDirectory)/../VSCodeExt && popd'
