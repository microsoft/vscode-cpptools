name: CI (Linux)

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-for-windows:
    uses: ./.github/workflows/jobs-compile-and-test.yml
    with:
      runner-env: ubuntu-22.04